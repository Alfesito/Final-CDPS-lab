##################################################################################################
# Details service
##################################################################################################
apiVersion: latest
kind: Service
metadata:
  name: details
  labels:
    app: details
    service: details
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: details
---
apiVersion: apps/latest
kind: Deployment
metadata:
  name: details-latest
  labels:
    app: details
    version: latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: details
      version: latest
  template:
    metadata:
      labels:
        app: details
        version: latest
    spec:
      containers:
      - name: details
        image: g40/details
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9080
        securityContext:
          runAsUser: 1000
---